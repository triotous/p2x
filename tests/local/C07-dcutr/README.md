# C07-dcutr

Deployment follows `../C01-smoke/run.sh`; configure the case fault/profile described by the plan. Collect raw NDJSON, topology, selected connection IDs, timings, hashes, terminal cardinality, RSS/FD/queue/permit samples, and cleanup state.
