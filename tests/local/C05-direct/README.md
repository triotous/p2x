# C05 direct path

Run `../C01-smoke/run.sh` to deploy the local processes, then configure the client for the direct path. Collect exchange transport, connection IDs, selected path, payload/hash/half-close result, setup duration, and final pending-open/worker counters. Fail on path disagreement or duplicate terminal output.