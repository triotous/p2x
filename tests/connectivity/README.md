# Connectivity spike harness

The local, namespace, and two-host procedures belong here. Runtime artifacts are written below `target/p2x-spike/` and are not committed.

The binaries currently provide the lifecycle/identity skeleton; connectivity cases are enabled as the networking phases land.
