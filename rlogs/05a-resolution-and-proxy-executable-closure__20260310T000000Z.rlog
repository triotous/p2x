session: 20260310T000000Z
plan: plan/05a-resolution-and-proxy-executable-closure.md
status: started
baseline: 65c7236 (plan added), reviewed implementation baseline 33f32ad
actions:
- Read coding-rust instructions and Plan 05a; plan file will not be modified.
- Inspected client resolver/connection/proxy ownership, exchange resolution, server ticket admission/proxy worker, protocol codecs, fuzz targets, and resolution runner.
- Ran cargo test --workspace --all-targets --all-features: passed.
decisions:
- Work will be split by ordered plan sections into separate commits.
- Keep changes narrow; use existing crates and test-hook convention.
- Maintain this append-only log and include it with the related phase commit.
blockers: none

phase 6.2 protocol vectors and framed fuzz coverage:
- Replaced descriptive resolve/proxy JSON with fixed body/frame hex vectors and lengths.
- Added protocol tests for all committed variants, exact re-encoding, frame lengths, trailing bytes, and unknown discriminants.
- Extended proxy fuzz target to call p2x-net async framed readers; added valid and malformed framed corpus seeds for resolve/proxy.
- Removed temporary vector-generation example after deriving committed bytes.
validation:
- cargo fmt --all: passed.
- cargo test -p p2x-protocol --lib: passed (27 tests).
- cargo test -p p2x-net --all-targets --all-features: pending before commit.
commit: phase 6.2 committed as de5abd0

phase 6.3 exchange resolution and admission controls:
- Added validated ResolutionLimits with documented hard maxima and wired configured values into the exchange admission ledger.
- Changed request idempotency digest to hash the exact canonical Resolve body.
- Added a fallible injected ticket-ID source seam and regression coverage for randomness failure without issuance.
- Added guarded exchange controls for first cached successful-response drop and bounded response hold, plus privacy-safe fault lifecycle event.
- Added resolver negative-cache supplied-clock decisions, short-circuiting, FIFO promotion ordering, queue/cache counters, and expiry regression coverage.
validation:
- cargo fmt --all: passed.
- cargo test p2x-client/p2x-exchange/p2x-protocol all targets/features: passed.
- cargo clippy for changed client/exchange/protocol targets with -D warnings: passed.
commit: phase 6.3 committed as 8461981

phase 6.4 bounded client route-open owner:
- Added apps/p2x-client/src/route_open.rs with bounded per-open ownership, request correlation, absolute deadlines, one resolve retransmission, cancellation, waiter promotion, path-action conversion, pre-handshake fallback routing, and one fresh-ticket retry classification.
- Added guarded CLI validation for route-open count/concurrency, delay, mutation, replay, direct-failure, and handshake-hold controls; normal defaults remain unchanged.
- Added route-owner resource counters and lifecycle test-fault record type; existing main swarm remains the sole executor.
validation:
- cargo fmt --all: passed.
- cargo test -p p2x-client --all-targets --all-features: passed (19 tests).
- cargo clippy -p p2x-client --all-targets --all-features -D warnings: passed.
- Integration of finite multi-open dispatch remains in the next owner phase; no live case is claimed by this commit.
commit: phase 6.4 committed as 4aef483

phase 6.5 connection ownership and server admission:
- Added per-server relay dial generations, singleflight/join actions, DCUtR generation tracking, waiter release, connection reuse action, surplus close marking, and generation tests to ConnectionManager.
- Added validated server proxy limit configuration and applied worker/replay settings to live behavior/channel ownership.
- Added immutable ticket signature verification candidate and owner-side claim/live validation API while preserving the compatibility authorize path.
- Added inbound behavior counters for lifecycle resource reporting.
validation:
- cargo fmt --all: passed.
- cargo test p2x-client/p2x-server all targets/features: passed.
- cargo clippy p2x-client/p2x-server all targets/features -D warnings: passed.
- Full worker split is not claimed until main dispatch consumes ValidationCandidate directly.
commit: phase 6.5 committed as acf63fa

phase 6.6 server verification/live consume and admission tests:
- Worker now performs immutable ticket envelope/signature/time verification and sends a ValidationCandidate; main owner performs current service/revision/claim checks and replay consumption.
- Added server proxy limit configuration (workers, per-client workers, replay capacity, skew) with hard validation and applied configured behavior/channel limits.
- Added replay binding-failure non-consumption and expiry-plus-skew retention tests.
validation:
- cargo fmt --all: passed.
- cargo test p2x-server all targets/features: passed.
- cargo clippy p2x-server all targets/features -D warnings: passed.
- Remaining product gaps: full multi-open dispatch and canonical process matrix.
commit: phase 6.6 committed as 47e7e3e

phase 6.7 finite owner controls and lifecycle fingerprints:
- Added finite diagnostic controls for proxy-open count, hidden hook validation, dropped-first-resolve retry, direct pre-handshake failure injection, replay attempt, mutation, delay, and handshake bounds.
- Added sequential finite empty-open aggregation with repeated fresh Resolve/ticket cycles and replay rejection handling.
- Added request/response fingerprints and cumulative issuance fields to resolution lifecycle records.
- Added direct-failure hook at ProxyStreamBehaviour before handler notification; existing path state machine performs relay fallback.
validation:
- cargo fmt --all: passed.
- cargo clippy --workspace --all-targets --all-features -D warnings: passed.
- cargo test --workspace --all-targets --all-features: passed.
- resolve-ticket-tcp live case: passed.
- resolve-ticket-quic live case: passed earlier after owner changes.
- canonical runner still has 13 status-2 cases; runner refactor is next.
commit: phase 6.7 committed as c5017ca

phase 6.7/runner evidence closure:
- Replaced the resolution runner's seven-case SUPPORTED early exit with explicit profiles for all 20 declared names.
- Added real runner coverage for idempotent Resolve, direct pre-handshake fallback, replay rejection, ticket-byte binding rejection, expiry rejection, sequential reuse, and 64 concurrent opens; these cases assert event cardinality, fingerprints, stream uniqueness, and selected-path order.
- Added explicit missing-evidence failures for binding matrix completion, revision mutation, resolve/proxy limits, exchange/server restart, and graceful-drain instead of silently certifying generic authorization.
- Fixed the runner privacy marker list to avoid treating public case labels as secrets while retaining token/raw-ticket/session/private-selector scans.
validation:
- cargo fmt/check/clippy/test workspace: passed after final changes.
- Executable resolution cases passed: existing TCP/QUIC seven, idempotent-resolve, direct-open-fallback, ticket-replay, ticket-expiry, and concurrent-opens/connection-reuse.
- ticket-bindings deliberately fails with missing complete matrix evidence.
- six process cases deliberately fail with missing orchestration evidence; no false green status is emitted.
commit: phase 6.7 committed as 7e5f228

phase 6.8 regressions, documentation, and final local gate:
- Added exhaustive unit-level ticket binding matrix coverage for issuer/client/server, tenant, upstream, selector fingerprint, registration revision, authorization revision, permissions, max streams, not-before, and wrong verification key; every mismatch remains outside replay state.
- Updated protocol/security/operations/test documentation to reflect implemented behavior, executable evidence, and explicit unresolved blockers. Plan files remain unchanged.
validation:
- cargo fmt --all -- --check: passed.
- cargo clippy --workspace --all-targets --all-features -- -D warnings: passed.
- cargo test --workspace --all-targets --all-features: passed.
- cargo deny check: passed.
- cargo tree -e features: passed.
- bounded nightly fuzz runs for all seven targets: passed (1000 runs each, no crash).
- tests/auth/local.sh --case all: passed.
- tests/registry/local.sh --case all: passed.
- tests/connectivity/local.sh --case all: passed.
- Evidence-backed resolution cases passed: existing TCP/QUIC, rejection cases, idempotent-resolve, direct-open-fallback, ticket-replay, ticket-expiry, connection-reuse, and concurrent-opens (64).
- Explicit resolution blockers remain nonzero: registration-revision-change, resolve-limit, proxy-limit, exchange-restart, server-restart, graceful-drain. The ticket-bindings same-process matrix is now executable and passing; its real-process representative is included.
- Deployment/platform phase remains owner-executed and incomplete: namespace/two-host/firewall/NAT/packet/platform/soak/package checks.
commit: phase 6.8 pending review

final binding-matrix correction:
- `ticket-bindings` now runs the exhaustive same-process ticket-admission test and records all required binding subcases, alongside its real ticket-byte mutation.

final runner correction:
- Changed `tests/resolution/local.sh --case all` to execute all 20 declared cases, collect failures, and return nonzero if any case fails instead of stopping at the first failure.
- Full aggregate run executed all 20: 13 evidence-backed cases passed; ticket-bindings process matrix plus registration-revision-change, resolve-limit, proxy-limit, exchange-restart, server-restart, and graceful-drain failed with explicit missing-evidence messages.
status: local Plan 05a Definition of Done not reached; Plan 06 must not begin.

binding matrix completion:
- `ticket-bindings` now runs and passes the complete unit matrix plus real ticket-byte mutation; all 13 required subcase names are included in the summary.
- Aggregate resolution rerun still reports six explicit orchestration blockers: registration-revision-change, resolve-limit, proxy-limit, exchange-restart, server-restart, graceful-drain.
validation: workspace static/tests remained green; no generated fuzz corpus noise retained.

final status:
- Branch is clean after the recorded phase commits.
- Plan 05a local gate is intentionally not complete: six declared process cases still require implementation/orchestration evidence.
- Plan 06 is blocked until those six cases pass; deployment/platform checks are also deferred to their owner-executed phase.

limit-hook attempt:
- A temporary held-worker process-hook attempt was tested and reverted because the finite client owner still opens sequentially, so it could not prove exact N+1 saturation; the runner continues to fail resolve-limit/proxy-limit explicitly.

phase 7.1 fresh-ticket recovery:
- Client proxy authorization now supports one guarded recovery after stale revision, peer setup/connection, or exchange timeout failure.
- Recovery invalidates the binding/selector metadata, discards the old proxy state, allocates a fresh Resolve request/ticket, and preserves the original setup deadline.
validation: cargo fmt, workspace clippy, p2x-client tests passed.
commit: ac7aaf9

phase 7.2 limits process orchestration:
- Added a second authenticated client identity to the resolution harness.
- Exchange response holding now retains each response channel without blocking auth/registry processing; server proxy worker holding is guarded, bounded, and cancellable.
- Proxy inbound admission rejection now retains the stream long enough to send a bounded `Rejected(limit.proxy_streams)` response and emits rejection evidence.
- `resolve-limit` passed with held N=1 and a real second client receiving `limit.resolve_requests`; no extra ticket state was allocated.
- `proxy-limit` passed with a held N=1 worker and a real second client receiving `limit.proxy_streams`; server emitted live worker and rejection evidence.
validation: both limit cases passed; shell syntax, workspace fmt/clippy/tests passed.
commit: 79098a0

phase 7.3 stale revision classification:
- Current server registration revision mismatches now return `registry.stale_revision` before replay consumption, while claim/tenant/authorization mismatches remain `auth.ticket_invalid`.
- Added a regression test proving a stale current revision rejects without allocating replay state.
validation: server Clippy and targeted stale-revision test passed.
commit: fe62031

phase 7.4 restart-safe server request IDs:
- Replaced server process-local request-ID restart from `1` with a fresh random nonzero start, preventing stale exchange idempotency collisions after a same-identity server restart.
- Targeted server stale-revision admission test passed; workspace fmt/clippy/tests passed.
- Initial server-restart process attempt exposed a second recovery issue: replacement server registration events and client fresh resolve still need deterministic owner correlation.
commit: 109204b

phase 7.5 restart/recovery process orchestration:
- Converted Resolve-to-Open delay into a nonblocking finite diagnostic stage so the swarm continues auth, reconnect, and registry processing while a grant is held.
- Added reusable exchange/server process starters and restart choreography with persisted identities, old registration cleanup, replacement registration checks, and fresh response fingerprints.
- Added client recovery for restart-time auth-session rejection and stale/ambiguous proxy failures.
- `server-restart` passed end to end: old registration is removed, the same server identity re-registers with a new revision, and the existing client fresh-resolves and authorizes.
validation: server-restart passed; workspace fmt/clippy/tests passed.
commit: 109204b

phase 7.6 exchange restart recovery:
- Client now defers transient `registry.not_found`/`registry.offline` during exchange recovery and retries fresh resolution non-blockingly within the original setup deadline.
- Exchange drain converts held Resolve channels to correlated `exchange.draining` rejection records.
- `exchange-restart` passed with the existing client/server processes and replacement registration.
validation: exchange-restart passed; workspace fmt/clippy/tests passed.
commit: 19762af

phase 7.7 graceful drain process matrix:
- Added independent server-drain, exchange-drain, and client-cancel subcases to the canonical runner.
- Server drain records owner-side `peer.draining` rejection for held proxy candidates.
- All subcases assert one terminal, readiness loss, zero final resources, and clean privacy scans.
- Full resolution aggregate passed all 20 declared cases.
validation: full `tests/resolution/local.sh --case all` passed.
commit: e7a1699

final local verification:
- Full `tests/resolution/local.sh --case all` passed all 20 declared cases.
- `graceful-drain` passed server-drain, exchange-drain, and client-cancel subcases.
- `cargo fmt --all -- --check` passed.
- `cargo clippy --workspace --all-targets --all-features -- -D warnings` passed.
- `cargo test --workspace --all-targets --all-features` passed.
- `cargo deny check` passed.
- `cargo tree -e features` passed.
- Auth, registry, and connectivity local suites passed.
- All seven nightly fuzz targets passed 1000 bounded runs each with no generated corpus noise.
- Documentation now describes the completed local process gates; only Plan 05a §7.4 deployment/platform checks remain owner-executed.
status: Plan 05a local Definition of Done reached for locally automatable scope; Plan 06 may begin after owner review of the deployment/platform handoff.
commit: pending phase review
